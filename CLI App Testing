# Use different Linux distros containers to check 'curl' cli tool version.
# Use 2 different terminal windows to start bash in both centos:7 and ubuntu:14.04, using '-it'.
# Learn the 'docker container --rm' command so you can save cleanup.
# Make sure curl is installed and on latest version for that distro : ubuntu- apt-get update && apt-get install curl, centos- yum update curl.
# Check curl --version 
